import { ResizableColumnDirectiveDirective } from './resizable-column-directive.directive';

describe('ResizableColumnDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableColumnDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
